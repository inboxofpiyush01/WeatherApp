Track A — device build: this workflow validates, tests, and uploads the Android APK/AAB. Configure signing secrets in CI before store delivery.

Track B — web app: a web surface was detected (web). Choose Vercel, Render, Railway, Fly, Netlify, or Docker Compose to publish the browser build. That track hosts HTTP, not the APK.
